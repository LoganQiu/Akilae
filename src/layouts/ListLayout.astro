---
import BaseLayout from "./BaseLayout.astro";

export interface Props {
  title?: string;
  subtitle?: string;
  description?: string;
}

const { title, subtitle, description } = Astro.props;
---

<BaseLayout title={title} subtitle={subtitle} description={description} pageType="list">
  <slot />
</BaseLayout>

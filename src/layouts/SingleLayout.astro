---
import BaseLayout from './BaseLayout.astro';
import PostNav from '../components/PostNav.astro';

export interface Props {
  title?: string;
  subtitle?: string;
  description?: string;
  keywords?: string[];
  author?: string;
  pubdate?: Date;
  moddate?: Date | null;
  prevPost?: any;
  nextPost?: any;
  ogImage?: string;
}

const { title, subtitle, description, keywords, author, pubdate, moddate, prevPost, nextPost, ogImage } = Astro.props;
---

<BaseLayout
  title={title}
  subtitle={subtitle}
  description={description}
  keywords={keywords}
  author={author}
  pubdate={pubdate}
  moddate={moddate}
  pageType="single"
  ogImage={ogImage}
>

    <slot />
    <PostNav prev={prevPost} next={nextPost} />

</BaseLayout>
